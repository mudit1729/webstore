"""Internal API callbacks — reserved for future use.

Currently the worker writes directly to DB and calls telegram_service.
This module is a placeholder for if/when we need HTTP-based worker→web communication.
"""
